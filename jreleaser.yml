release:
  github:
    overwrite: true
    changelog:
      formatted: ALWAYS
      preset: conventional-commits
      links: true
      contributors:
        format: "- {{contributorName}} ({{contributorUsernameAsLink}})"

signing:
  active: ALWAYS
  armored: true

#announce:
#  twitter:
#    active: RELEASE
#    status: "🚀 Wilo {{projectVersion}} has been released! {{releaseNotesUrl}}"

distributions:
  wilo:
    active: RELEASE
    type: BINARY
    stereotype: CLI
    artifacts:
      - path: target/{{ projectName }}-{{ projectVersion }}.tar
      - path: target/{{ projectName }}-{{ projectVersion }}.tar.bz2
      - path: target/{{ projectName }}-{{ projectVersion }}.tar.gz
      - path: target/{{ projectName }}-{{ projectVersion }}.tar.snappy
      - path: target/{{ projectName }}-{{ projectVersion }}.tar.xz
      - path: target/{{ projectName }}-{{ projectVersion }}.zip
        extraProperties:
          universal: true
      - path: target/{{ projectName }}-{{ projectVersion }}.noarch.rpm
      - path: target/{{ projectName }}-{{ projectVersion }}.src.rpm
    brew:
      active: RELEASE
      dependencies:
        jq: "null"
      skipTemplates:
        - README.md.tpl
      commitAuthor:
        name: julb
        email: julien@julb.me
